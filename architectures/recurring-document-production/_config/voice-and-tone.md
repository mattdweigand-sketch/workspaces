# Voice and Tone (pointer + report overlay)

<!--
The team's CORE voice lives once in the shared team voice file, not here:
  _shared-config/voice-and-tone.md
  (from this workspace under workspaces/<name>/, that path is ../../_shared-config/voice-and-tone.md)

The draft stage reads the shared file for the team's voice. This local file holds ONLY what is
specific to recurring reports and notices, layered on top of that shared voice. Most report-specific
guidance is structural and belongs in format-patterns.md (lead with the headline figure, one paragraph
per segment, etc.); put genuine *voice* nuances unique to reports here, and keep it thin.

If the team has no shared voice file yet, create it once from _shared-config/voice-and-tone.md during
setup. See Constraint 05 (Voice Architecture). This file is L3 reference.
-->

## Team voice
See `_shared-config/voice-and-tone.md` — the team's core written voice. Do not duplicate it here;
reference it. Update the team voice there and every workspace stays consistent.

## Report-specific voice notes (overlay)
[Only what differs for recurring reports and notices beyond the team's standard voice — e.g., a touch
more formality than a quick email, the steward-to-reader register. Keep this thin. Structural patterns
for the report (what leads, section order) live in format-patterns.md, not here.]
