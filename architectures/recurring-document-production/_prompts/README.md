# Reusable Prompts

<!--
This folder holds reusable prompt fragments for common tasks within this
workspace. These are NOT copy-paste prompts. They are structured instruction
blocks that you reference from stage contracts or invoke as needed.

Examples of what goes here:
- A reconciliation prompt that ties headline figures to the platform export
- A constraint-check prompt that audits a draft against your constraints file
- A variance-explanation prompt for drafting the "what changed and why" section

Start empty. Add prompts as you discover tasks you repeat every cycle.
The prompt becomes worth saving when you have written it more than twice.
-->
