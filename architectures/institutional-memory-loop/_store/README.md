# Institutional Memory Store

This store holds validated lessons and pattern updates.

- `records/`: one record per validated event.
- `patterns.md`: reusable patterns that should inform future work.

Do not add unvalidated causal claims to this store.
