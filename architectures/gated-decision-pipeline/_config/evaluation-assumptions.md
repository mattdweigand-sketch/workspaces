# Rough-Check Assumptions

<!--
ANNOTATION: The standard assumptions the evaluate stage uses for its back-of-
envelope check. The point is consistency: every item gets the same rough treatment,
so evaluations are comparable. These are deliberately coarse.

CRITICAL: This is a filter, not the full analysis. The numbers produced from these
assumptions catch obvious non-starters; they do not value an item. The real analysis
happens downstream with the model or spreadsheet that computes the numbers and
verified data (Constraint 09, Platform Boundary). Never let a screen figure
masquerade as the full analysis.

This is L3 reference, refreshed as conditions move (note the as-of date).
-->

## As-Of Date
[When these assumptions were last refreshed. Stale assumptions produce misleading
evaluations.]

## Standard Assumptions
[Coarse, by item type. Examples:
- Baseline rates or rules of thumb by type
- A rough growth or change assumption
- Cost or ratio rules of thumb]

## Resourcing Assumptions
[Standard rough resourcing for the check: typical effort, cost, or timeline.
Coarse, for the rough estimate only.]

## Target Bar
[The rough outcome an item must plausibly clear at the screen level to merit
pursuit. This is the screening bar, intentionally rough — not the decision-makers'
required outcome.]

## Use Notes
[Reminders for the evaluate stage: label every output number as a rough screen
estimate; state which assumptions were used; do not refine these per item — if an
item needs custom assumptions to look good, that is a downstream question, and the
answer at screen level is decline or advance-and-find-out.]
