# Thresholds

<!--
ANNOTATION: The testable go/no-go rules the evaluate stage applies. Two kinds:
automatic disqualifiers (any one ends the evaluation as a decline) and the weighing
criteria (how fit and the rough check combine into a recommendation). Write these so
the same item gets the same answer no matter who evaluates it — that consistency is
the whole point. See Constraint 04 (Session Consistency).

This is L3 reference, stable across items.
-->

## Automatic Disqualifiers
[Conditions that end the evaluation immediately with a decline. Any single one is
sufficient. Examples:
- Type outside scope
- Domain the team will not enter
- Size outside the range
- A structure the team will not do
- A legal, compliance, or risk flag visible at screen level
State each as a yes/no test.]

## Weighing Criteria
[When no disqualifier trips, how fit and the rough check combine into a
recommendation. Examples:
- Fit on all core dimensions + rough check clears the bar → advance
- Strong rough check but partial fit → advance only with a named rationale
- Full fit but the rough check misses badly on standard assumptions → decline or need-more
Define what "clears" and "misses badly" mean in your terms.]

## Need-More Triggers
[What makes an item "need-more" rather than decline: a specific missing fact whose
answer would flip the recommendation. Distinguish this from "decline" — need-more
is reserved for a genuine, identifiable unknown, not general uncertainty.]

## Decline-Reason Taxonomy
[A short standard list of decline reasons (off-strategy, mispriced, out-of-domain,
structure, timing, etc.) so the decline log is consistent and the team can see its
rejection patterns. The decide stage tags each decline with one.]
